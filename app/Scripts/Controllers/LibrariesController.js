angular.module('controllers')
    .controller('LibrariesController', [
        '$scope',
        function ($scope) {
            // Execute pretty manually
            prettyPrint();
        }]);